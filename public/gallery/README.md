# 馆藏纪念插画

这三张图片由本次 DigitalMuseum 界面迭代通过内置图像生成工具制作，均为 1086 × 1448（3:4）。对应提示词保存在同名 `.prompt.txt` 中。

- `paper-study.png`：纸页与金线。
- `thread-study.png`：缎带与圆环。
- `bridge-study.png`：纸桥与玻璃。

它们是通用装饰资产，不是历史现场、成果证据或基于用户真实会话生成的个性化内容。界面必须保留相应说明。图像不含原始用户数据；没有使用参考网站的展出图片。

纹理在同一画廊场景内按 URL 复用；静态展览导出暂不包含这些装饰图。

## gathered-pages.png · 聚成一册

用于“记录归入一个项目入口”的抽象纪念插画。纸页、黄铜细线与鼠尾草绿档案门框表达从分散到归组；不是历史现场，也不是精确数量图。由 imagegen 生成，不包含个人原话、日期、路径、项目名或品牌标识；运行时按草稿 artwork 标识显式选择，不参与旧封面的 hash 池。

## gathered-pages-v2.png · 聚成一册（翻阅版）

2026-09-13 通过内置 imagegen 生成，1086 × 1448。以旧图为材质与色调参考，新构图采用散页、黄铜装订环、打开的档案册和陶质书托，表达归组之后仍可逐页回看。完整提示词见 `gathered-pages-v2.prompt.txt`，不含个人资料或会话正文。

现有 `gathered-pages` 标识加载此版本，旧图保留；不增加 hash 封面池，也不增加展品。图像仍为纪念性艺术表达，不能作为历史现场或准确数量的依据。绑定记录与私有故事保存在本地运行时目录，不包含在本资产目录。

## everyday-steps.png · 从日常走近原理

2026-09-13 通过内置 imagegen 生成，1086 × 1448。纸咖啡杯、逐步上升的纸台阶与玻璃透镜，表达从熟悉的生活场景逐步理解内部原理。旧 `gathered-pages-v2.png` 仅作材质与色调参考；完整去私密提示词见 `everyday-steps.prompt.txt`。

## continuous-light.png · 让原来的光延续

2026-09-13 通过内置 imagegen 生成，1086 × 1448。连续浅紫纸带穿过宽窄陶质框架，黄铜支撑托住纸边，表达保留原貌、修复衔接。旧 `gathered-pages-v2.png` 仅作材质与色调参考；完整去私密提示词见 `continuous-light.prompt.txt`。

这两张图均不包含原始私人会话、项目身份、原话、日期或统计。仅由本地已保存草稿的白名单标识选择，不加入通用 hash 池。它们是开发阶段制作的纪念插画，不代表产品已具备自动绘图能力；不会进入现有原始记录静态导出。私人绑定与来源快照继续保存在 Git 忽略的运行时目录。
