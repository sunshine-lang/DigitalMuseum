# Digital Museum · AI 人生档案馆

本地优先的单人工具：把散落在本机的 AI 协作痕迹整理成可核对的经历档案，最终导出可分享的静态展览。

## Language

**Agent 会话记录（第一燃料）**:
第一阶段唯一的第一等导入源：本机各类 Agent 产品留下的会话转录——以人机对话为主体、带时间戳与项目归属（cwd）的 JSONL（Codex、Claude Code、pi Agent、dsh、OpenClaw 等）。调试转储、进程心跳等非对话痕迹不算；笔记上传与 Git 仓库导入通道已彻底删除。
_Avoid_: 多源导入、通用记录导入、调试转储

**档案库（Archive）**:
一名用户全部会话证据的持久集合，是数据的唯一属主。同步按来源幂等；源内容变化时替换证据快照，保留事件的用户判定；清空档案库是独立的两步确认操作。
_Avoid_: 数据库、仓库（与 Git 仓库混淆）

**阶段（Stage）**:
对档案库的一个命名时间窗视图，供回顾与开展使用；时间结构由同步后的数据推导，删除阶段只删视图、不动档案。
_Avoid_: 导入范围、数据属主

**Skill 机会（Skill Opportunity）**:
从会话档案中淘出的、用户反复执行却未沉淀为工具/规则的流程线索；由确定性重复统计产生（逐字锚定、频次、跨项目分布），是否沉淀由用户判断。语义级聚合（同意图不同措辞）属模型能力，只产 candidate。
_Avoid_: 自动生成 skill、语义聚类（当前阶段）

## Codex 读取边界

`codex-evidence-v2` 支持旧事件与 item_completed，按轮次选择消息来源并去重；只读单一可定位续接链。未知消息或缺失历史明确报错，不生成零消息的已核实档案。它仍输出确定性统计，不是完整多轮原文素材包。三份候选故事为预先保存的样本，真实库尚未因本次修复全量同步。详情见 `docs/plans/2026-09-14-codex-session-compatibility.md`。
