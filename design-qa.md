# 纵深画廊验收 · 2026-09-11

以下第一节为初版历史记录，最新故事接入验收见文末。

本轮范围：在现有 `/exhibition` 中替换展示交互，继续使用真实档案 API、选展状态与既有静态导出。不包含模型生成完整经历或个性化绘图；三个通用 AI 插画已明确标注为装饰。

## 视觉依据与环境

- 用户选定参考：[Atmospheric Depth Gallery](https://tympanus.net/Tutorials/DepthGallery/)。
- 参考截图：`/private/tmp/dm-depth-qa/reference.png`，通过本机 Ego 浏览器捕获并实际查看。
- 实现截图：`docs/assets/readme/exhibition.png`，以及 `/private/tmp/dm-depth-qa/implementation-desktop.png`。
- 桌面两者均为 1440 × 900 CSS px / 图片像素，DPR 1，无浏览器外框；同为第一个展项、没有打开详情。
- 实现使用隔离 E2E 样本（narrative-1/2/3），参考是原站 GOLDEN。原站色卡说明改为项目名、日期、首问摘录与阅读入口，是本项目的明确功能适配，不作逐字或逐像素复制。
- 其他状态：阅读对话框 `/private/tmp/dm-depth-qa/implementation-reader.png`（1280 × 720），手机 `/private/tmp/dm-depth-qa/implementation-mobile.png`（390 × 844 视口、整页截图）。手机截图中的固定底栏在截图展开位置覆盖部分长页，只代表该视口位置；滚动与阅读按钮已通过实际浏览器测试。
- 本机真实档案也通过 Ego 浏览器打开验证；真实档案截图只保留在临时目录，不写入 README。

## 比较与修正记录

1. 首次同一输入中比较参考和实现完整截图：主图的 3:4 比例、左侧位置和大幅留白成立；发现 P1：未来展品投影进入右侧正文，降低可读性。
2. 修正：收小未来展项横向偏移，并将 Three.js 可见区域限制在画面左 62%，让正文区域独立。
3. 在同一视口、同一 E2E 数据和同一展项重新截图，并与参考同一输入复核：主图约占高度 70%、中心位于横向 37%，远处展项局限在图片区域，文字和阅读按钮没有被遮挡。原 P1 已关闭。随后将小字号辅助文字统一加深至 #60665b / #5d6655，改善浅色背景的可读性；在真实档案页面另行截图核对。
4. 另查看完整阅读对话框与手机长页：正文、原文行、指纹展开入口均保持 HTML 文字；手机无横向溢出，操作栏可用。完整截图中文字足够清楚，阅读对话框截图承担了正文和出处的细部核查，本轮不再另做图像放大裁切。

## 五项视觉检查

- 字体：保留档案品牌的 Source Serif 4 / 中文宋体阅读层，与 JetBrains Mono 小型标签；中英文项目名允许换行，不截掉正文。原站以小型色卡标注为主，本项目增加较大的阅读标题属于预期差异。
- 布局：左索引、大幅竖图、右正文和底部导航；目录独立滚动。原站没有的选展、出处和导出控件已完整接入。
- 色彩：以奶油、淡紫、蓝灰跟随展项变化，正文保持深色。较原站金黄背景更克制，延续档案材料的色调；保留空间导航而没有复制原站色卡功能。
- 图像：三张 1086 × 1448 的真实 raster 生成资产均已检查；没有以 CSS/SVG 假画替代插画。采用共享纹理，最多显示临近展项，不为每条档案重复上传相同纹理。
- 文案：只展示现有档案，不把计数与时间核实扩大为完整故事已核实。原问来自现有派生档案的摘录，可能截短；界面明确说明。装饰插画不声称对应历史现场。

## 功能与代码验证

- `npm run typecheck`、`npm run lint`、`git diff --check` 通过。
- `npm run test:backend`：Ruff + 47 项通过；本轮没有修改后端。
- `npm run test:local`：构建、3 项渲染冒烟、10 项静态导出检查通过。
- E2E 共 13 个不同用例：第一轮 11 过、2 个证据行顺序断言失败；改为定位实际包含原话的证据行后，7 项画廊专项全部通过，其余 6 项此前通过且相关逻辑未再修改。布局修正后再次运行三段导航用例通过。
- 测试覆盖：前后导航、边界禁用、目录跳转、重新选展、阅读对话框按钮/Esc/背景关闭和焦点返回、主动轻量模式、reduced-motion、手机、WebGL 失败降级、导出内容与敏感信息取消。
- 人工浏览：本机真实档案读取、三段选展、详情阅读、前后导航与轻量/3D 模式切换。
- 运行日志：构建保留 Three.js 大 chunk 提示（通过动态 import 在进入画廊后加载）；WebGL 被测试主动禁用时出现预期的 context error，随后按预期进入轻量模式。Ego 浏览器记录到一条 hydration 提示，差异字段为扩展注入的 `data-immersive-translate-page-theme`；未改浏览器扩展，手动交互流程正常。该环境提示与隔离测试中主动禁用 WebGL 的日志分开记录。本轮不声称完成性能基准测试。

## 后续边界

- 静态导出继续采用原有午夜编年阅读版，无脚本、无外链，不含 3D 交互和新装饰插画。
- 模型回顾生成、完整会话证据快照、本人确认故事版本属于后续内容迭代，未被此次视觉初版实现。
- P3：可在真实使用反馈后进一步优化长标题的字阶、插画种类和章节过渡。没有剩余 P0/P1/P2 项。
- 本轮没有提交或推送 GitHub。

final result: passed


## 第二轮：项目入口与日期记录

- 展览准备间改为项目卡片，支持月份筛选、整项目选择与展开逐日勾选。主画廊按最近活动排序，一个可信项目身份一个入口；封面按身份固定。详情有日期导航，逐条保留正文、状态和全部原文依据。
- 项目身份来自来源元数据；旧记录缺失 source_key 或 occurrence 时，只读已保存的证据文档头并验证 SHA-256。无迁移、无历史数据清理。Claude 新同步只在纳入会话均有一致 cwd 时记录该字段；旧 Claude 的转义目录不能当作真实路径。
- 真实本地 API 复核：54 条记录 → 23 个展项，其中 6 条归属不明记录独立保留。DigitalMuseum 的 8 条 Codex 记录归为一个入口；其旧 Claude 记录仍为会话目录入口，并标注跨 Agent 归属待确认。
- 视觉：已在同一工具输出中对照参考截图与 1440×900 当前桌面截图，保留竖画、留白、左索引、右说明的构图。路径在正文缩为末两级，完整路径保留在悬浮提示，避免长路径抢占画面。详情日期按钮与正文/出处分区明确。390×844 手机截图已查看，横向无溢出；全页截图中的固定底栏处在原视口底部，因此遮住一部分长页截图，不表示内容无法滚动阅读。
- 当前截图：`docs/assets/readme/exhibition.png`；临时留存 `/private/tmp/dm-depth-qa/project-desktop.png`、`project-reader.png`、`project-mobile.png`。均为隔离测试数据，不包含用户真实会话正文。
- 浏览器状态：原 Ego 预览任务空间已由用户接管，本轮停止操作该空间。视觉与交互验收使用项目既有的隔离 Playwright 测试；真实数据另经只读 API 核对。未声称在用户浏览器中完成本轮手动操作。
- 最新验证：Ruff + 53 个后端测试、15 个完整 E2E 全部通过；typecheck、lint、构建、3 个渲染冒烟和 10 个静态导出检查通过。E2E 覆盖三天同项目一个入口、日期切换原文、封面在日期/位置改变后稳定、月份过滤与导出一致、重新选展保留选择、原生 dialog 焦点与三路关闭、手机、reduced-motion、WebGL 回退与导出风险检查。修复了项目半选状态的 aria-pressed=mixed。
- 第一轮 E2E 在后端兼容处理文件仍变动时出现档案列表读取失败；后端完成后完整重跑 15/15 通过。WebGL 禁用用例的 context error 是预期输出。
- 已知历史数据问题：迁移后的 claims 表缺少 occurrence_id 外键，快照替换可能遗留旧 claim。本轮以只读原文回退支持这些记录，未删除或修复历史数据；因此只承诺项目入口归组，不承诺消除记录内部既有的重复来源。
- 本轮没有新增依赖，没有模型调用，没有提交或推送 GitHub。前端 3001 与本地 API 8010 已恢复并返回 HTTP 200。

当前项目分组结果：passed（旧档案归属不明确与历史重复来源保留上述边界）。


## 第三轮：已保存故事接入（2026-09-12，当前版本）

- 范围：把上一轮私有故事草稿接入当前项目详情，支持阶段故事/原始记录切换、章节导航、全文出处与待补充问题。没有模型调用、编辑/确认写入或数据库迁移；草稿保持 candidate，不并入系统 verified 事件。
- 数据：只读 `outputs/retrospectives/*/story.json`，通过真实 project_key 关联；个人正文与来源均在 Git 忽略目录，未硬编码进公开源码。运行 API 已独立核对返回 DigitalMuseum 的 5 章、17 份来源快照、2026-08-25 至 2026-09-12 范围与 candidate 状态。
- 选展与导出边界：故事正文展示自己的完整范围，并明确不随月份过滤改写；现有静态 HTML 导出仍只有所选原始记录。草稿文件独立于档案库 ZIP 和清空操作，README 已说明单独备份。
- 视觉验收：沿用当前画廊纸张色、宋体正文、墨绿选中态。桌面左右并列叙述/出处，手机纵向阅读；章节切换会将焦点和滚动位置移动到新章节标题。实际查看 1440×900 桌面、390×844 手机截图，正文/引文/状态可区分，没有横向溢出。截图为隔离测试材料，保存于 `outputs/qa/story-reader-2026-09-12/`，不进入公开素材。
- 验证：69 个后端测试和 Ruff 通过；原有 15 个 E2E 与新增 4 个故事用例通过，修正截图取景后桌面/手机 2 项再次通过；typecheck、lint、构建、3 个渲染冒烟和 10 个导出检查通过。
- 调试记录：测试会话 fixture 起初缺少 rollout- 文件名前缀，修正后进入故事链路；随后发现前端故事 API 地址缺少 /api/v1，已修复并通过故事 4 项复测。WebGL 故意禁用时的 context error 仍是预期降级证据。
- 最终本地前端 3001 与 API 8010 已启动，展览和故事 API 均返回 HTTP 200。未操作已由用户接管的 Ego 空间，未提交或推送 GitHub。

当前故事接入结果：passed。后续自动生成、编辑核对、本人确认与故事导出仍待独立迭代。

## 第四轮：成果在前、过程在后（当前展示样本）

- 使用已有真实故事的一段项目归组经历，外层呈现具体工作标题、短概述、来源类型与纪念插画；内层先目标，再用户选择/Agent 执行/材料记录，最后结果与边界。保留阶段故事和原始记录，仍是 candidate。
- 新增可选 exhibit 结构及摘要目录；旧草稿兼容。直达地址按 story ID 与真实项目键匹配，不全局重写档案事件。候选经历仍不进入原始记录静态导出。
- 纪念插画“聚成一册”使用去私密抽象提示词生成，表达记录归组，不代表历史现场或精确记录数量；不改变旧封面 hash 池。
- 74 个后端测试/Ruff、21 个完整 E2E、typecheck、lint、构建、3 个渲染冒烟和 10 个导出检查通过。E2E 使用临时数据；WebGL 被测试禁用时出现预期 context error 并成功回退。
- 真实材料另在新的隔离浏览器上下文中只读验收，桌面 1440×900、手机 390×844：直达、外层阅读、目标/过程/结果、原话展开、记录切换、关闭/焦点返回通过，无 JS 页面错误、无横向溢出。实际完整视口截图已查看，保存在 outputs/qa/outcome-first-2026-09-12/（desktop-cover/detail/process.png，mobile-cover/detail/process.png）；均为本地实际材料，未加入公开素材。
- 当前真实档案的所选日期与历史经历的日期分别标明；经历里“8 条”明确属于当时复核记录，当前原始记录数量可以继续变化。
- 前端 3001/API 8010 已恢复，直达预览与目录 API 均返回 200。无新增依赖、无额外 Agent、无提交推送或部署。自动整理、本人确认、故事导出、用户价值和市场验证均不在本轮完成范围。


## 第五轮：选定 A 现代私人展馆接入正式版本

### 来源与对照状态

- 来源：`outputs/prototypes/2026-09-12-three-directions/qa/A-desktop.png`、`A-reader.png`、`A-mobile-reader.png`。
- 正式真实数据截图：`outputs/qa/selected-A-integration/desktop-multi-final.png`、`desktop-single-final.png`、`desktop-reader.png`、`mobile-reader.png`、`mobile-cover.png`。
- 桌面源图/正式截图同为 1440×900 CSS px 与图片像素，DPR 1；手机同为 390×844。WebGL 使用 2160×1350 内部绘制缓冲再映射到 1440×900 CSS 视口，这不改变最终截图密度。
- 主展与阅读均对照同一段 DigitalMuseum 项目归组经历。正式相邻图片来自真实项目，原型来自三段固定材料；没有为像素匹配伪造邻居或替换正式数据。单件直达不拿它的缺少邻居当成景深失败，多件视图另行验收。
- 源图与正式图已在同一比较输出实际查看；阅读与手机也按相同方式对照。主图采样另与同尺寸原生 HTML 图片作直接对比，属于图像细节复核；文字在完整阅读截图中清楚，无需另做图像裁切。

### 发现、修复和再次对照

- [P0，已关闭] ResizeObserver 可能赶在纹理图像就绪前读取 naturalWidth。加入空图像保护，ready 标记移到首次真实绘制完成后；后续浏览与回归无此错误。
- [P2，已关闭] 首轮画框阴影上下出现硬边、旧向下箭头样式误套在单件提示上。修正阴影蒙版尺寸和继承样式，正式截图中不再存在。
- [P2，已关闭] 近景主图比 A 原生图片明显偏软。检查确认源图 1086×1448、CSS/绘制画布初始均 1440×900、无 CSS blur。改用原始纹理级线性采样，并以 1.5–2x 有界绘制密度改善细线；`desktop-single.png` 为修复前，`desktop-single-linear.png` 为中间采样对照，`desktop-single-final.png` 为最终，`desktop-single-html.png` 为原生图片对照。根任务及协调任务均实际对照并关闭该问题，未重新生成图片。

### 五项视觉与交互结论

- 字体：同源系统无衬线栈，主标题 30–44px、正文 16px，恢复 A 的层级、字重和深色主操作。长标题可换行并保留可滚动阅读入口。
- 布局：相同左侧画框中心与右说明布局，真实前后遮挡层次；阅读为小图、目标/过程/结果与出处双栏。手机纵向阅读，控件与正文无横向溢出。
- 色彩：固定 A 的浅色展馆与深绿文字/主按钮，不再随旧图片色板变色。纸张阴影、面板圆角与原型相符。
- 图像：复用既有本地资产，正确按内框裁切且无拉伸，近景清晰。WebGL 挂载边角和原生 CSS 阴影存在轻微渲染差异，列为非阻断 P3；没有额外图像打磨。
- 文案：用户原话、Agent 说明和材料结果保持区分。正式 API 没有提供可信进展类别时显示“已保存经历/待本人确认”，不照搬原型的“本地已实现”标签。单件/最后一件有明确提示，数据来源不足时保留原始记录。
- 必要差异：移除 A/B/C 比较栏后使用完整视口，画框相对纵向中心下移约 40px；正式阅读模式行占用一行，增加的可用高度抵消了这部分空间。保留重新选展、导出、目录和视图切换，均是正式能力而非视觉漂移。

### 验证和边界

- 后端 74 项/Ruff 通过；test:local（构建、3 个渲染、10 个导出）、typecheck、lint 通过。未改后端业务。
- 23 个 E2E 场景覆盖通过：首轮 22 过，单件提示的 exact 文本断言因子元素被误判，修正测试后通过；最终又复测减少动效、A 画框导航/释放、长标题/窄窗 3 项。原始记录选展、月份、导出敏感检查与无脚本契约保留。
- 最终采样密度下，另用隔离浏览器对真实数据只发 GET：多件画框点击走近/阅读、关闭与焦点、手机经历阅读均通过，pageerror 为空；记录为 `outputs/qa/selected-A-integration/live-checks.json`。
- 单件与多件真实页面在新 in-app Browser 标签检查，未接管旧用户空间。README 图使用隔离样本，私人真实截图只在 outputs/。
- 没有新增依赖、额外代理、数据库重加工、模型调用、本人确认流程、提交、推送或部署。静态导出仍是既有无脚本原始记录阅读版。

final result: passed
