# 真实数据大考 · v0.3 复考记录（2026-08-25）

> 上次大考：`docs/gate/real-data-exam-2026-08-24.md`（四过三，「愿意发给朋友」未达标）。
> 本次为 v0.3 重构（S1 档案库为根 → S5 里程碑叙事，提交 51bfe00…07baec0）落地后的复考。
> 机器侧数据已于 2026-08-25 采集（见下）；**人工字段以「待用户填写」标出，判定归用户**。
> 通过标准见 PRD v0.3 §11（五项）。

## 数据范围与同步（机器侧，2026-08-25）

**档案**：无阶段边界——一键同步本机全部会话产品（ADR-0001 档案库为根）。

**同步结果**（`POST /api/v1/archive/sync`，多次执行含增量）：

- Claude Code：3 个项目 → 6 段经历；Codex：10 个项目 → 20 段经历（另 2 段跨源聚合）；
- pi：5 个项目 9 个会话 → 6 段经历（S4 当日接入当日验证）；
- dsh：项目目录已迁移（cwd 失效）被确定性排除——与 8-24 大考的 Codex 目录迁移问题同源，已列为产品待办；
- 同步全程 **0 失败、0 重复**（幂等跳过与快照替换经多次增量验证）。

**当前口径**：18 个项目、34 段事件，**全部 verified**（会话时间戳/计数为确定性读数），跨度 2026-07-01 → 2026-08-26（约 2 个月，短于 PRD 的 ≥6 个月口径，如实注明）。

**叙事差异化**（S5 出口条件）：34 张展卡 **34 种句式零重复**；里程碑角色分布：首日 10 / 最密集日 4 / 收尾 7 / 日常推进 16 + 单日即兴变体。

## 通过标准与现状（PRD v0.3 §11）

| # | 标准 | 现状（机器侧） | 用户判定 |
|---|---|---|---|
| 1 | 一键同步 ≥6 个月全部会话转录，无失败无重复 | 同步 0 失败 0 重复已验证；实际跨度约 2 个月（短于 6 个月，如实注明） | 待用户填写 |
| 2 | 事前手写关键事件，系统找回 ≥6/10 | 8-24 封存清单（离职/资料整理/项目实施）仍可对照；v0.3 数据为全量会话，召回情况待逐项核对 | 待用户填写 |
| 3 | 人工介入负担 ≈ 0（无核对队列，仅统计异议次数） | 核对队列已不存在；异议操作次数：0（复考期间未使用异议通道） | 待用户填写 |
| 4 | P0「系统核实但实际错误」= 0 | 待用户抽查 | 待用户填写 |
| 5 | 产出的静态展览愿意发给至少一位朋友 | 导出链路可用（脱敏扫描 + 单文件 HTML）；叙事差异化已达标；意愿归用户 | 待用户填写 |

## 结论

机器侧五项中第 1、3 项已由本轮重构直接满足；第 2、4、5 项需用户按上表填写判定。**在用户判定完成前，不把 v0.3 表述为「已通过大考」。**

## 附录：机器复核证据（2026-08-27，供用户判定参考；判定栏仍归用户）

> 复核环境：当日清档后经 `POST /archive/sync` 一键重同步的当前档案——36 段（Codex 20 / Claude Code 6 / pi 6 / 跨源聚合 4），跨度 2026-07-01 → 2026-08-27，全部 verified、同步 0 失败 0 重复。以下全部为只读机器证据，不构成判定。

### #2 召回对照（封存清单 → 当前「仅会话」口径档案）

| 封存事件 | 当前档案证据 | 备注 |
|---|---|---|
| 离职 | 间接信号：产品经理课件 4 天学习轨迹（07-29 / 08-09 / 08-11 / 08-14，Codex） | v0.2 时用户曾接受间接证据（当时另有 112 会话完整轨迹 + 14 份笔记，通道清剿后间接信号减弱，判定请按当前口径） |
| 资料整理 | 「在 wiki 与 Claude Code 协作 2026-07-28」直接找回 | 14 份技术笔记已随通道清剿删除，不在档案中 |
| 项目实施 | 多项目直接找回：DigitalMuseum×4、hipaw×5、制作Skill×3、新能源agent项目×2、Agent-Reach×1、agent-blueprint×1 | — |

按封存基准（3 件，找回 ≥2 且无编造）评估：直接找回 2 件（资料整理、项目实施），无编造记录；「离职」是否按间接证据算找回，归用户判定。

### #4 独立复算（不经后端，回读本机会话原文重算）

方法：按 AGENTS.md 记载的确定性规则（共享扫描骨架 + 各适配器分类规则 + `<` 开头系统行过滤 + UTC 按本机时区归日），直接读取 `~/.claude/projects`、`~/.codex/sessions`（仅 `thread_source=="user"`）、`~/.pi/agent/sessions` 原文，对全部 26 段单源事件独立重算四项读数（归日日期、会话数、用户消息数、首条真实用户消息），与档案 claim 逐项比对；4 段跨源聚合事件的组成源已包含在上述 26 段内。

结果：**26/26 完全一致，0 差异**（含多会话日按最早会话取首条消息的口径）。

### #5 导出产物（已生成待用户过目）

- 文件：`outputs/digital-museum-我的-Agent-协作档案.html`（17,313 字节；outputs/ 为 git 忽略目录）
- 机械校验：无 `<script>`、无 `<link>`/外链、含 `<style>`、含 2026 年 7/8 月章节、含「未随本页导出」诚实注记
- 导出过程脱敏扫描零命中（未触发人工确认弹窗）
- 「愿意发给朋友」的判定动作归用户：请双击打开该文件（断网验证），再决定是否发送
